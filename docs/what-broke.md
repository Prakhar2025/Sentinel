# What Broke (living log)

Every entry is a genuine failure hit during the build, appended in real time with its fix. Entries are never rewritten, polished, or invented after the fact. This file is the source for the "what broke at 2 AM" pitch segment and is freshness-enforced by a pre-commit hook.

| Date (IST) | Phase | What broke | Root cause | Fix |
|------------|-------|------------|------------|-----|
| 2026-08-22 | 0 | System `python` was 3.9.10; venv created from it cannot run the py3.12 stack | PATH points at an old Python install while 3.12.10 exists via the `py` launcher | Pin all tooling to `py -3.12` (Makefile, docs); venv recreated from 3.12.10 |
| 2026-08-22 | 0 | `pip install` hung and failed with DNS errors on `pypi.ngc.nvidia.com` | Machine-wide pip config adds the NVIDIA NGC index as an extra index and that host no longer resolves | Set `PIP_CONFIG_FILE` to a nonexistent path for all install commands (bypasses global config); documented in Makefile workflow |
| 2026-08-22 | 0 | `OperationNotPageableError` calling Bedrock `list_foundation_models` | boto3 1.4x: the operation is not paginated; script used `get_paginator` | Single direct call; discovered in the boto3 docs shape |
| 2026-08-22 | 0 | `ParamValidationError: Invalid type for parameter byInferenceType, value: ['ON_DEMAND']` | The API takes a scalar string, not a list | `byInferenceType="ON_DEMAND"` |
| 2026-08-22 | 0 | `AttributeError: 'VerifyResult' object has no attribute '__dict__'` on output | dataclass declared with `slots=True` (no instance dict) | `dataclasses.asdict()` for serialization |
| 2026-08-22 | 0 | gpt-oss 120B/20B returned empty text (non-JSON parse failure) | They are reasoning models: a 128-token cap is consumed entirely by thinking before any output token | Raise maxTokens to 512 for verification calls; Phase 5 must check stopReason before treating empty output as non-compliance |
| 2026-08-22 | 0 | Nova Lite and GLM-5 responses failed `json.loads` despite being valid JSON | Both wrap JSON in markdown code fences; none of the five candidates support native `responseFormat` on Bedrock | Fence-stripping parser in the verification script; Phase 5 design updated to require fence-stripping + jsonschema gate |
| 2026-08-22 | 0 | Llama 3.3 70B Converse call rejected with ValidationException | Model is listed in-region but its base ID is not invocable (needs a cross-region inference profile) | Dropped from the fallback chain; `zai.glm-5` keeps the final slot (measured 703 ms, valid JSON) |
| 2026-08-23 | 1 | `python -m sentinel.generate` failed with ModuleNotFoundError: no module named sentinel | src-layout package is not on sys.path outside pytest (pytest injects pythonpath=src, plain python does not) | Added `[build-system]` to pyproject and an editable install (`pip install -e . --no-deps`) to `make setup` |
| 2026-08-23 | 1 | First dataset run put all 100 fraud events in the train split (calibration and test had zero fraud, making the evaluation impossible) | Greedy split filler was rewritten without the `counts[chosen] += len(members)` update, so split deficits never shrank and train absorbed every component | Restored the count update; split module now also gets a test asserting all three splits receive fraud events |
| 2026-08-23 | 1 | Softening the Zipf exponent made merchant concentration worse (57% share at a=1.1 vs 40% at a=1.6) | numpy zipf has an unbounded heavy tail; clipping draws to the pool size piles the whole tail onto the last merchant, and a smaller exponent means a heavier tail | Replaced zipf+clip with explicitly normalized power-law weights (rank^-1.2 over the 45-merchant pool); measured top share 29% |
| 2026-08-23 | 1 | CI install job failed: pip tried to git-clone the private repo and died with "could not read Username" | Lockfile was regenerated with plain `pip freeze` inside the venv, which recorded the local editable install as a `git+https://...Sentinel.git@<sha>` dependency; CI runners have no credentials to clone the private repo | Regenerate the lockfile with `pip freeze --exclude-editable` so it contains only third-party pins; the package itself is imported via pytest's pythonpath, no install needed in CI |
| 2026-08-23 | 2 | Ring clusters lost their identity ratio (1.83 identities per device instead of 2.7+) and cluster extraction crawled (replay tests took 15s) | The cluster BFS traversed THROUGH merchant nodes; a big merchant is adjacent to hundreds of unrelated clean customers, so every cluster absorbed them and diluted the signal | Merchants are now counted as leaves but never traversed; identity linkage flows through device/phone/vpa/email only. Measured after fix: ring ratio restored, cluster_stats p95 = 0.64 ms |
| 2026-08-23 | 2 | Taint never reached identities that linked into a fraud neighborhood AFTER the outcome event (test expected 0.36 for a later customer, got 0.0) | Taint spread ran only at outcome time as a single BFS; nodes attached later never received it | GraphStore tracks taint sources (rebuilt from confirmed_fraud attrs on load) and re-spreads whenever an upsert creates new edges while sources exist |
| 2026-08-23 | 2 | Phone parser rejected valid "0091..." international-prefix numbers; VPA parser rejected single-character local parts | The 0091 branch checked length 13 but 0091 + 10 digits is 14; the VPA regex used {2,64} where the doc 04 contract is {1,64} | Length check corrected to 14; regex loosened to {1,64}; both pinned by table-driven tests |
| 2026-08-23 | 3 | First unconstrained weight calibration inflated amount_pattern (F6) to weight 32, contradicting the docs/05 design that keeps F6 minor | Coordinate ascent on the synthetic set found a shortcut: ring amounts (uniform 500-2,000 INR) partially separate from the log-normal clean amounts, so F1 optimization piles weight onto the weakest, most distribution-dependent feature | Published a feature-prior cap (WEIGHT_CAPS, amount_pattern <= 10) as a design constraint in the calibrator; recalibrated weights are dominated by new-identity-burst (35), taint (20), ratio (13) |
| 2026-08-23 | 3 | Locked thresholds came out review == block (empty REVIEW abstention band) | The score distribution is strongly bimodal (clean mass at 40-47, fraud at 49+), so the "90% of max F1" review rule collapsed onto the block threshold | Review threshold now locks at the smallest score where queue precision reaches 0.25 (one-in-four triage floor); locked at review=42, block=49 |
| 2026-08-23 | 3 | Three feature-test expectations failed against correct code (taint 1.0 vs 0.36, velocity 1 vs 2, precision 1.0 vs 0.5) | F3 is cluster-max taint (the source's 1.0 is in-cluster); the velocity probe forgot its own 99-minute offset against the 72h window; the precision fixture used default weights that score clean rows far below threshold | Fixed the tests with correct semantics; the code was right, the expectations were wrong |
| 2026-08-23 | 4 | POST /v1/feedback always 404 despite a valid verdict_id | The service looked the verdict up by event_id; verdict_id and event_id are different keys and only the event_id query existed | Added AuditStore.get_verdict_by_id and wired the feedback route to it; contract test now exercises the full flow |
| 2026-08-23 | 4 | Phone mask shape did not match the documented example (+9198XXXX5678) | The mask kept the first 3 digits instead of the first 4 as specified in docs/06 | Fixed the mask to keep 4 leading digits; example pinned in the unit test |
| 2026-08-23 | 4 | Live ingest response was missing schema_version and explanation_status fields that the contract test expected | verdict_to_json carries the engine payload; those two fields only existed on stored rows | Added both to the live response with the store's SCHEMA_VERSION constant; contract test asserts them |
| 2026-08-23 | 5 | Bedrock timeout default (5 s) would kill every real explanation | Phase 0 measured gpt-oss-120b at 7.2 s on a trivial prompt (reasoning overhead); a real explanation is heavier, so the default was set below the measured floor without anyone noticing | Raised the default to 30 s with a comment citing the measurement; explanations are async so the larger bound costs nothing user-facing |
| 2026-08-23 | 5 | rebuild_store failed on the second run with WinError 32 (file in use) | SQLAlchemy's connection pool keeps the SQLite file open; Windows refuses to unlink an open file (Linux would not) | Added AuditStore.close() (engine.dispose) and closed the store inside rebuild_store before returning; test pins the rebuild-twice flow |
| 2026-08-23 | 6 | Baseline test rows were extracted after the full chronological replay, leaking the future into the baselines | The first evaluate.py draft had a separate _test_rows pass over the finished graph; features must be captured at each event's own timestamp to match online serving | Rows are now cached during the single replay at scoring time; caught in self-review before any evaluation number was produced |
| 2026-08-23 | 6 | Logistic baseline crashed with "n_splits greater than the number of groups" on small fixtures | GroupKFold(3) assumed at least 3 ring/customer groups; unit fixtures have 2 | n_splits = min(3, unique groups) with a no-CV fallback for fewer than 2 groups |
| 2026-08-23 | 6 | CI quality job failed (tests errored with SystemExit "locked model config missing") while the same suite passed locally | The evaluation tests called run_evaluation() with the default config path; the locked calibration lives in the gitignored evaluation/ directory, which exists on the dev machine but never in a fresh CI checkout | Tests now build a self-contained test config (default weights, explicit thresholds) in tmp storage and pass it explicitly; the harness's refusal to run without a locked config is preserved and still asserted |
| 2026-08-23 | 7 | Cluster and entity views returned "unavailable" in the console although the store held 1,000 verdicts | The service booted with an empty in-memory graph: seeded data was written directly to the store, and nothing replayed stored events on startup, so every restart lost the graph until new traffic arrived | create_app now rebuilds the graph from stored events (event sourcing, chronological by ingestion order); after restart /readyz reports 3,243 nodes and clusters render |
| 2026-08-23 | 8 | Replay subtitle printed the literal text "&middot;" instead of a middle dot; cluster-graph labels collided; calibration table showed duplicate "40-40" rows | HTML entities inside a JS template literal are plain text, not JSX; graph labels were centered above nodes with no collision logic; equal-count decile slicing split tied scores across buckets, repeating range labels on the bimodal distribution | Used the real middot character in the template string; graph labels now sit radially outward with side-aware anchoring (vision-verified: zero overlaps); calibration buckets are built from whole score groups so ranges are unique by construction |
